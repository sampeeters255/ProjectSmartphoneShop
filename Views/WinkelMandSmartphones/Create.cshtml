@model ProjectSmartphoneShop.Models.WinkelMandSmartphone

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>WinkelMandSmartphone</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WinkelmandId" class="control-label"></label>
                <select asp-for="WinkelmandId" class ="form-control" asp-items="ViewBag.WinkelmandId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SmartphoneId" class="control-label"></label>
                <select asp-for="SmartphoneId" class ="form-control" asp-items="ViewBag.SmartphoneId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
